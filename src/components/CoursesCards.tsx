import { ViewContainer } from './ui/view-container';

// TODO: Add cards in this section and style it accordingly

const CoursesCards = () => {
  return (
    <section id="courses">
      <ViewContainer></ViewContainer>
    </section>
  );
};

export default CoursesCards;
